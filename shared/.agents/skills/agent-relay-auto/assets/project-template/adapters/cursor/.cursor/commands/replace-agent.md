Use the agent-relay-auto Skill to replace the participant assigned to one role. Show current task and default bindings, ask for current-task/project-default/both scope, and select or register a same-role identity. Preserve immutable identities and history. Confirm any old writer stopped, obtain explicit authorization, then use the bundled workflow_state.py helper with the freshly read revision. Do not modify product code in this management operation. After replacement, tell the new participant to invoke continue separately.
