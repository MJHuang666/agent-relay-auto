# Tasks

Copy `_template/` to a unique `TASK-YYYYMMDD-NNN/`. Set its language from `PROJECT_STATUS.md`, create the requirement and plan, and set `active_task` only when the task is ready to be worked.

`TASK-EXAMPLE-001` is a reference example and must not be activated or treated as a real task.
